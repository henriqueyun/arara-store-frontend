import { Grid } from "@mui/material";

export default function ProductsFilter(props) {
    const { products } = props;
    // should create an expandable area for each field of product
    return (
        <Grid>
            Products Filter =^_^=
        </Grid>
    )
}